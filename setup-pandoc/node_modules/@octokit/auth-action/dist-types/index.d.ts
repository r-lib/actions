import { type Types as AuthTokenTypes } from "@octokit/auth-token";
import type { StrategyInterface } from "@octokit/types";
export type Types = {
    StrategyOptions: never;
    AuthOptions: never;
    Authentication: AuthTokenTypes["Authentication"];
};
export declare const createActionAuth: StrategyInterface<[
], [
], Types["Authentication"]>;
