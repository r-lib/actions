name: test-fix-864-2

on:
  push:

jobs:
  test-fix-864-2:
    runs-on: macos-latest
    steps:
      -
        name: checkout
        uses: actions/checkout@v4
      -
        name: setup-r
        uses: the-mad-statter/actions/setup-r@fix-864-2
      -
        name: rscript
        run: Rscript -e 'print("hello")'